apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-mq-config
  namespace: {{ .Values.nameSpace }}-{{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
data:
  qmgrdef-k8s.mqsc: |
    DEFINE QLOCAL(TEST.LOCAL.QUEUE)
    